<!DOCTYPE HTML>
<html>
    <head>
        <title>YelpBars</title>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
        <link href="/stylesheets/main.css" rel="stylesheet">
        	
        <!-- Includes jquery framework which is needed for bootstrap js framework -->
	    <script src="https://code.jquery.com/jquery-3.2.1.js" integrity="sha256-DZAnKJ/6XZ9si04Hgrsxu/8s717jcIzLy3oi35EouyE=" crossorigin="anonymous"></script>
        <!-- Includes the Bootstrap JS framework -->
    	<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js" integrity="sha384-Tc5IQib027qvyjSMfHjOMaLkfuWVxZxUPnCJA7l2mCWNIpG9mGCD8wGNIcPD7Txa" crossorigin="anonymous"></script>
    </head>
    
    <body>
        
     <nav class="navbar navbar-inverse">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-nav-collapsed" aria-expanded="false">
                <!-- EACH SPAN IS RESPONSIBLE FOR BARS ON THE BURGER IN THE COLLAPSED MOBILE VIEW
                whatever data target is set too is what will be shown 
                we use the # to target the id to show what's hidden and shown in the burger menu-->
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
     	        </button>
                <a class='navbar-brand' href="/">YelpBar</a>
            </div>
            
            <!--so it collapses to the burger menu for the smaller screens-->
            <div class="collapse navbar-collapse" id="bs-nav-collapsed">
                <ul class="nav navbar-nav navbar-right">
                    <% if (!currentUser){ %>
                        <li><a href="/login">LogIn</a></li>
                        <li><a href="/register">Sign Up</a></li>
                    <% } else { %>
                        <!--good route would be to a profile page-->
                        <li><a href="#"><strong>Signed In As: </strong><%= currentUser.username %></a></li>
                        <li><a href="/logout">Log Out</a></li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>
    
    <div class="container">
        <!--just  having error exist isn't enugh for us to display so you check for length to 0 because sometimes it will return
        an empty array and [] isn't falsey most of the time it is an empty array if [] == true-->
        <% if(error && error.length > 0) { %>
        <div class="alert alert-danger" role="alert">
            <%= error %>   
        </div>
        <% } %>
        <% if(success && success.length > 0) { %>
        <div class="alert alert-success" role="alert">
            <%= success %>   
        </div>
        <% } %>
    </div>

        
